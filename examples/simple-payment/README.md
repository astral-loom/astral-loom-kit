# Simple Payment Example

This is a minimal example demonstrating how to use `astral-loom-kit` to:
1. Retrieve network configurations.
2. Build a Stellar payment transaction.
3. Catch and elegantly map Stellar errors.

## How to run

First, install the dependencies:
```bash
npm install
```

Then, run the example:
```bash
npm run start
```
